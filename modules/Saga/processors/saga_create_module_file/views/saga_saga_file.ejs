import { all, call, put, takeLatest } from "redux-saga/effects";
import {
  _SUCCESS,
} from "./constants";

function* <%= module %>Saga() {
  yield all([]);
}


if(false) console.log(_SUCCESS, call, put, takeLatest);
export default <%= module %>Saga;
