import { _ERROR, _SUCCESS } from "./constants";

const initialState = {};

export function <%= module %>Reducer(state = initialState, { type, payload }) {
  switch (type) {
    default:
      return state;
  }
}

if(false) console.log(_ERROR, _SUCCESS);
